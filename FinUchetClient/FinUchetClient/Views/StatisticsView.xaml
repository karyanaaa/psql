<UserControl x:Class="FinUchetClient.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="#F5F7FA">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="📈 Аналитика и статистика" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       Margin="0,0,0,20"/>

            <!-- Панель выбора периода -->
            <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,20" BorderBrush="#E0E0E0" BorderThickness="1">
                <WrapPanel>
                    <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <DatePicker x:Name="StartDatePicker" Width="120" Height="30" Margin="0,0,10,0" SelectedDateChanged="DateRange_Changed"/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker x:Name="EndDatePicker" Width="120" Height="30" Margin="0,0,20,0" SelectedDateChanged="DateRange_Changed"/>
                    <Button Content="Обновить" Width="80" Height="30" Background="#3498DB" Foreground="White" Click="Refresh_Click"/>
                </WrapPanel>
                
            </Border>

            <!-- Блок с итогами -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#E8F8F5" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="💰 Доходы" Foreground="#27AE60" FontSize="14"/>
                        <TextBlock x:Name="TotalIncomeText" FontSize="28" FontWeight="Bold" Foreground="#27AE60" Text="0 ₽"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="#FEF5E8" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="📉 Расходы" Foreground="#E67E22" FontSize="14"/>
                        <TextBlock x:Name="TotalExpenseText" FontSize="28" FontWeight="Bold" Foreground="#E67E22" Text="0 ₽"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="#E8F4FD" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="⚖️ Баланс" Foreground="#2980B9" FontSize="14"/>
                        <TextBlock x:Name="BalanceText" FontSize="28" FontWeight="Bold" Foreground="#2980B9" Text="0 ₽"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Круговая диаграмма -->
            <Border Grid.Row="3" Background="White" Padding="20" Margin="0,0,0,20" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="📊 Расходы по категориям" FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <lvc:PieChart x:Name="ExpensesPieChart" Height="300" Margin="0,0,0,10" LegendLocation="Right"/>
                </StackPanel>
            </Border>

            <!-- Линейная диаграмма -->
            <Border Grid.Row="4" Background="White" Padding="20" Margin="0,0,0,20" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="📈 Динамика доходов и расходов по месяцам" FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <lvc:CartesianChart x:Name="IncomeExpenseChart" Height="350" LegendLocation="Top">
                        <lvc:CartesianChart.Series>
                            <lvc:LineSeries x:Name="IncomeSeries" Title="Доходы" PointGeometrySize="8" StrokeThickness="2"/>
                            <lvc:LineSeries x:Name="ExpenseSeries" Title="Расходы" PointGeometrySize="8" StrokeThickness="2"/>
                        </lvc:CartesianChart.Series>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- Кнопки экспорта -->
            <Border Grid.Row="5" Background="White" Padding="15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="📄 Экспорт в CSV" Background="#2ECC71" Foreground="White" Padding="15,10" Margin="0,0,10,0" Click="ExportToCsv_Click" FontSize="13"/>
                    <Button Content="📊 Экспорт в Excel" Background="#3498DB" Foreground="White" Padding="15,10" Click="ExportToExcel_Click" FontSize="13"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>